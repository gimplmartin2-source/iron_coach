# IronCoach - 2026-04-01

## Gespräche heute
- **20:38 - 20:42**: Intensives Troubleshooting mit Martin zum Render-Deployment der Trainings-Tracker WebApp
- Martin zeigte mir Live-Logs vom Render-Deploy, die Fehler `ENOENT: no such file or directory, stat '/opt/render/project/src/public/index.html'` anzeigten
- Gemeinsam debuggten wir das Pfad-Problem mehrere Commits lang

## Arbeitsschritte
- **20:30-20:35**: Ersten Fix implementiert - statischer Pfad prüft jetzt auf index.html Existenz (nicht nur Verzeichnis)
- **20:35-20:38**: Commit `4dfde2f` gepusht, aber Logs zeigten Code wurde nicht korrekt deployt (Caching-Problem)
- **20:38-20:40**: Zweiter Ansatz - expliziter Pfad mit `process.cwd() + '/webapp/public'` (Commit `dd9beb7`)
- **20:40-20:42**: Render zeigte immer noch alte Version - Caching-Problem erkannt
- **20:42-20:44**: Dritter und finaler Fix:
  - `render.yaml` erstellt mit Umgebungsvariable `PUBLIC_PATH=/opt/render/project/src/webapp/public`
  - Server-Code angepasst um `process.env.PUBLIC_PATH` zu nutzen mit Fallback
  - `startCommand: cd webapp && node server.js` konfiguriert
  - `render.yaml` ins Root-Verzeichnis verschoben (war vorher in webapp/)
  - Commit `cffb661` gepusht mit vollständiger Konfiguration

## Entscheidungen
- **Mehrere Iterationen nötig**: Einfacher Pfad-Fix war nicht genug, weil Render Caching-Probleme hatte
- **Umgebungsvariablen als Lösung**: Statt komplexer Pfad-Logik, explizite PUBLIC_PATH Variable gesetzt
- **Root-Level render.yaml**: Entscheidung, die Render-Konfiguration ins Root zu verschieben, da Render sie nur dort findet
- **cd webapp && node server.js**: Server soll aus dem webapp-Verzeichnis starten, damit Pfade stimmen

## Status WebApp
- **Aktueller Stand**: Render-Deployment mit expliziter Pfad-Konfiguration gepusht (Commit `cffb661`)
- **Erwartetes Ergebnis**: `index.html` sollte jetzt unter `/opt/render/project/src/webapp/public` gefunden werden
- **Offene Punkte**: Ergebnis des Deploys muss noch verifiziert werden (Martin muss prüfen ob es jetzt funktioniert)
- **URL**: https://trainings-tracker-7kuw.onrender.com

## Erkenntnisse
- Render nutzt Caching aggressiv - mehrere Deploys nötig bis neue Version greift
- `__dirname` und `process.cwd()` verhalten sich auf Render anders als lokal
- Shell-Zugriff ist bei Free-Tier deaktiviert, daher kein direktes Debugging auf dem Server möglich
- `render.yaml` MUSS im Repository-Root liegen, nicht in Unterverzeichnissen
- Dokumentation sollte Umgebungsvariablen-Setup explizit erwähnen für zukünftige Deployments
