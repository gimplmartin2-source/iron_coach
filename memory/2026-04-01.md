# 2026-04-01 - Bugfix: Übungen/Workouts nicht sichtbar nach Google Sync

## Problem
Nach Google-Login und Daten-Restore wurden keine Übungen und Workouts angezeigt, obwohl die Daten synchronisiert wurden.

## Ursache
In `app.js` wurde nach erfolgreichem Restore `window.location.reload()` aufgerufen, aber `sessionStorage.setItem('restoreAttempted', 'true')` blieb im sessionStorage gespeichert. Nach dem Reload war das Flag immer noch auf `'true'`, wodurch der Code-Block zum Laden der Übungen und Workouts übersprungen wurde.

## Fix Teil 1: Restore-Flag
In `webapp/public/app.js` wurden zwei Stellen geändert:

1. Automatisches Restore - vor `window.location.reload()`:
```javascript
sessionStorage.removeItem('restoreAttempted');
window.location.reload();
```

2. Manueller Restore mit Token - gleiche Änderung

## Zusätzliche Fixes (nach Console-Error Report)

### Problem: API 500 Fehler + Array-Fehler
Die Console zeigte:
- `/api/exercises:1 Failed to load resource: the server responded with a status of 500`
- `exercises.forEach is not a function`
- `preloadExerciseGifs is not defined`

### Fix Teil 2: Robusteres `loadExercises()`
```javascript
async function loadExercises() {
    try {
        const res = await apiFetch('/api/exercises');
        if (!res || !res.ok) {
            console.error('API Fehler:', res?.status);
            exercises = [];
            return;
        }
        const data = await res.json();
        // Sicherstellen dass data ein Array ist
        if (Array.isArray(data)) {
            exercises = data;
        } else {
            console.error('Ungültige API Antwort:', data);
            exercises = [];
        }
        // ...
    }
}
```

### Fix Teil 3: Robusteres `loadWorkouts()`
- Gleiche Array-Prüfung wie bei `loadExercises`
- Prüfung ob `exercises` ein Array ist vor `exercises.find()`
- Fehler-Logging verbessert

### Fix Teil 4: `preloadExerciseGifs` Stub
```javascript
// GIFs vorladen für bessere Performance (Stub)
function preloadExerciseGifs() {
    console.log('📱 Preload Übersprungen');
}
```

### Fix Teil 5: Server `/api/exercises` verbessert
- Try-catch um den gesamten Endpoint
- Sicherstellung dass `rows` ein Array ist
- Video Resolver Fehler abfangen
- Im Fehlerfall trotzdem Daten zurückgeben

## Ergebnis
- App ist robuster gegen API-Fehler
- Keine JavaScript-Laufzeitfehler mehr
- Selbst bei Server-500-Fehlern fließt die App nicht ab

## Commits
- `5aa810c` - Erster Fix: restoreAttempted Flag
- `53e9e5e` - Zusätzliche Fixes: Robustere Fehlerbehandlung
