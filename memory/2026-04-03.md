# IronCoach - 2026-04-03 (Donnerstag/Freitag)

## Gespräche heute

- **14:45 Uhr**: Session start - Martin fordert mehrere Änderungen:
  - Token-Popup bei Google-Login entfernen
  - Stattdessen automatisches Restore vom Google Drive beim Laden
  - Falls kein Backup existiert: Standard-Übungen anlegen lassen

- **15:03 Uhr**: Martin fragt "hast du gepusht" - Bestätigung mit Commit-ID

- **15:19 Uhr**: Martin meldet Fehler: "sqlite error table workouts has no column named duration_seconds"
  - Auch Kraft-Übungen konnten nicht mehr gespeichert werden

- **15:31 Uhr**: Wichtige Anweisung von Martin: **"Immer die Push-Nummer mit dazu schreiben wenn du etwas pushst"**

- **15:57 Uhr**: Google Token abgelaufen Fehler (`Invalid Credentials`)
  - Problem: Das OAuth Access Token im JWT war ungültig
  - Lösung: Neuer Login für frisches Token

- **16:12 Uhr**: Martin bestätigt "ok hat funktioniert" - Token-Problem gelöst

- **16:18 Uhr**: Martin bittet um Anpassung der Standard-Übungsnamen
  - Sollten exakt gleich sein wie die Video-Dateinamen

- **16:38 Uhr**: Martin fragt nach verfügbaren Videos auf GitHub
  - Ich zeige ihm die Liste aller MP4-Dateien und GIFs

---

## Arbeitsschritte

### 14:45-15:03: Automatisches Restore & Default-Übungen
- `app.js` modifiziert:
  - `restoreFromDrive()` komplett überarbeitet - kein `prompt()` mehr
  - Automatisches Restore mit JWT-Token beim Laden
  - Neue Funktion `createDefaultExercises()` mit 19 Standard-Übungen
  - Init-Logik angepasst für automatisches Restore bei Google-Login
- `server.js` gefunden und korrigiert:
  - Problem: `/api/restore` hatte fehlerhafte Weiterleitung via `app._router.handle()`
  - Fix: Direkte Implementierung der Restore-Logik statt Redirect

### 15:19-15:23: Hotfix für `duration_seconds`
- Problem: Render hatte alte DB noch ohne die neue Spalte
- Lösung: Fallback-Mechanismus eingebaut in `POST /api/workouts`
  - Automatische Erkennung wenn Spalte fehlt
  - Dynamisches ALTER TABLE oder Fallback-INSERT ohne Spalte
- Commit `[bb25dde]` gepusht

### 16:18-16:28: Standard-Übungen an Video-Namen angepasst
- `seedDefaultExercises()` in `server.js` aktualisiert:
  - Vorher: `"Bankdrücken (Langhantel)"` → Nachher: `"Bankdrücken Langhantel"`
  - Vorher: `"Kreuzheben"` → Nachher: `"Klassisches Kreuzheben"`
  - Vorher: `"Klimmzüge"` → Nachher: `"Klimmzüge Untergriff"`
  - Vorher: `"Fliegende (Butterfly)"` → Nachher: `"Butterfly"`
  - Vorher: `"Rudern (Langhantel)"` → Nachher: `"Kurzhantel Rudern"`
  - Vorher: `"Schrägbankdrücken"` → Nachher: `"Schrägbank drücken"`
  - Vorher: `"Face Pulls"` → Nachher: `"Face Pulls Kabelzug"`
  - Vorher: `"Bizeps-Curls"` → Nachher: `"Bizepscurls Kurzhantel"`
  - Vorher: `"Französisches Trizeps"` → Nachher: `"French Press"`
  - Vorher: `"Hammer Curls"` → Nachher: `"Hammercurl Kurzhantel"`
  - Vorher: `"Trizeps-Drücken"` → Nachher: `"Trizeps Pulldown Maschine"`
  - Vorher: `"Plank (Unterarmstütz)"` → Nachher: `"Plank"`
  - Vorher: `"Russische Twist"` → Nachher: `"Russian Twist"`
  - Vorher: `"Schulterdrücken"` → Nachher: `"Schulterdrücken Kurzhanteln"`
  - Vorher: `"Seitheben"` → Nachher: `"Kurzhantel Seitheben"`
  - Vorher: `"Frontheben"` → Nachher: `"Frontheben Kurzhantel"`
  - Vorher: `"Seitst"` (Tippfehler?) → Nachher: `"Side Plank Extreme"`
  - Vorher: `"ADIM-Core (für Gleitwirbel)"` → Entfernt (nicht in Videos)
  - Neue Übungen hinzugefügt: `"Becknen Heben"`, `"Bauchroller"`, `"Crunches Maschine"`, etc.
- INSERT-Befehl angepasst für `exercise_type` Speicherung
- Commit `[b7a8411]` gepusht

---

## Entscheidungen

- **Entschieden**: Token-Popup entfernen statt verbessern - direkte automatische Integration ist besser UX
- **Entschieden**: Fallback statt hartem Fehler bei `duration_seconds` - robustere Lösung für Render-Deployments
- **Entschieden**: Zukünftig immer Commit-Hash mitliefern bei Push-Benachrichtigungen
- **Entschieden**: Standard-Übungen exakt auf Video-Dateinamen abstimmen für konsistentes Matching

---

## Status WebApp

### Heute implementiert:
- ✅ **Token-Popup entfernt** - Keine manuelle Eingabe mehr nötig
- ✅ **Automatisches Frontend-Restore** - Google Drive Backup beim Laden
- ✅ **Automatisches Backend-Restore** - `/api/restore` Endpoint gefixt
- ✅ **Standard-Übungen als Fallback** - 37 Übungen für neue User
- ✅ **`duration_seconds` Hotfix** - Robustes Error-Handling für fehlende Spalte
- ✅ **Übungsnamen an Videos angepasst** - Konsistentes Naming

### Video-Integration:
- **40 MP4 Videos** in `public/`
- **30 GIFs** in `GIF Trainingsübungen/`
- **37 Standard-Übungen** jetzt mit Video-Matching-Namen

### Offene Punkte:
- Refresh-Token-Logik für länger gültige Google-Auth (niedrige Priorität)
- Mobile Responsiveness weiter verbessern

---

## Commits heute (mit Hash!)

```
[2ba0a00] Remove token popup, add automatic Google Drive restore + default exercises
[8fe868f] Fix /api/restore endpoint - direct implementation instead of redirect  
[af23d0e] Auto-restore on page reload for Google users
[a1a7ade] Add exercise_type column and support in API
[68ef975] Add migration after restore to fix exercise_type column
[4f460d0] Add migration to /api/restore/drive endpoint
[a8db1de] Add PUT /api/exercises/:id endpoint for editing exercises
[8e13f74] Add duration_seconds column and support in API
[bb25dde] Hotfix: Auto-create duration_seconds column if missing
[b7a8411] Update default exercises to match video filenames exactly
```

---

## Erkenntnisse

- **Commit-Hashes wichtig**: Martin will exakte Referenz für jeden Deploy zum Nachvollziehen
- **Render DB ist ephemeral**: SQLite-Dateien werden bei neuen Deploys zurückgesetzt
- **Google OAuth Token ablauf**: Access Tokens laufen ab, brauchen Refresh-Token-Logik
- **Video-Matching**: Konsistente Namen zwischen Übungen und Video-Dateien sind essentiell
- **Fallback-Strategie**: Bei Render ist es sicherer, fehlende DB-Spalten dynamisch zu ergänzen statt Migrationen zu vertrauen

---

*Memory-Eintrag erstellt: 2026-04-03 15:25 CET (Cron-Job)*
*Quelle: Sessions-History + Subagent-Logs*
